# TGAT Attention Analyzer

Analyze TGAT attention weights and validate pattern authenticity.

## Integration
- Expects embeddings with `attention_weights` (e.g., from `embeddings/attention_topk.parquet`).
- Can augment IRONFORGE discovery explainability.
